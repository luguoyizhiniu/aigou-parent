server:
  port: 7001
eureka:
  instance:
    hostname: localhost #eureka实例的地址
  client:
    fetch-registry: false #是否注册到注册中心
    register-with-eureka: false #是否从注册中心获取注册信息
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka